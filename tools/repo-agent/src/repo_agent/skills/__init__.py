"""Skills — high-level, composable operations that consume the MCP tools.

Each skill is available both as an MCP tool (registered by ``server.py``) and
as a CLI subcommand (``repo-agent <skill> ...``) so contributors can run them
without an MCP client.
"""
