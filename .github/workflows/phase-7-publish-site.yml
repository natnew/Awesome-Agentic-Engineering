name: Phase 7 — Publish site

on:
  push:
    branches: [main]
    paths:
      - "README.md"
      - "RUBRIC.md"
      - "ANTI-PATTERNS.md"
      - "CONTRIBUTING.md"
      - "CHANGELOG.md"
      - "appendix/**"
      - "tools/repo-agent/**"
      - ".github/workflows/phase-7-*.yml"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  regenerate:
    name: Regenerate docs/ and open PR on drift
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # tools/repo-agent is local-only automation and is not published to
      # this repository. Skip cleanly when it is absent; the site is then
      # regenerated locally by the maintainer and committed under docs/.
      - name: Check for repo-agent
        id: tooling
        run: |
          if [ -f tools/repo-agent/pyproject.toml ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "::notice::tools/repo-agent is not present in this repository (local-only automation); site regeneration skipped."
          fi

      - name: Set up Python
        if: steps.tooling.outputs.present == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install repo-agent
        if: steps.tooling.outputs.present == 'true'
        run: python -m pip install --quiet -e tools/repo-agent

      - name: Regenerate docs/
        if: steps.tooling.outputs.present == 'true'
        continue-on-error: true
        run: python -m repo_agent workflow render-site

      - name: Detect drift
        id: drift
        if: steps.tooling.outputs.present == 'true'
        run: |
          if git diff --quiet -- docs/; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Open or update regeneration PR
        if: steps.drift.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore(docs): regenerate site"
          title: "chore(docs): regenerate site"
          body: |
            Automated regeneration of `docs/` from the Markdown sources of truth.

            <!-- awesome-agentic:phase-7:regenerate -->
            Triggered by push to `main`. Review the diff, then merge to publish
            the updated site via GitHub Pages (`main` / `/docs`).
          branch: chore/regenerate-docs-site
          base: main
          add-paths: docs
          delete-branch: true
